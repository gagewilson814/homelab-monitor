# Copy to /etc/homelab-monitor/agent.env (chmod 600) and adjust as needed.
# Both vars are optional - the agent runs fine with neither set.

# AGENT_PORT=8080
# HOMELAB_SERVICES=jellyfin:32400,plex:32401
